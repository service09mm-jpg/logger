// Роут, через який проходить увесь обмін з Google: Auth.js сам обробляє і
// початок входу, і повернення з підтвердженням. Свого коду тут немає —
// обробники приходять готовими з конфігурації.
import { handlers } from "@/lib/auth";

export const { GET, POST } = handlers;
